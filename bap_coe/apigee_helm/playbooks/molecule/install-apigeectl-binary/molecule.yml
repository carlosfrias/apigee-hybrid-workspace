---
dependency:
  name: galaxy
driver:
  name: delegated
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: apigeectl-install-binary
provisioner:
  name: ansible
verifier:
  name: ansible
