---
dependency:
  name: galaxy
driver:
  name: delegated
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: 08-create-external-ip-address
provisioner:
  name: ansible
verifier:
  name: ansible
