---
# This is an example playbook to execute Ansible tests.
- name: GCE Instance Create | Verify
  hosts: all
  gather_facts: no

  tasks:
  - name: GCE Instance Create | Example assertion
    assert:
      that: true
