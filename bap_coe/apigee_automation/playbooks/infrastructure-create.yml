---
- name: Create infrastructure for private cloud in GCP
  hosts: localhost
  connection: local

  vars_files:
    - ~/.apigee-secure/apigee-hybrid-sensitive-attributes.yml
    - attributes.yml

  collections:
    - bap_coe.gcp_management
    - bap_coe.bootstrap_runtime

  roles:
    - { role: gcp_project_create, tags: [ 'project' ] }
    - { role: gcp_iam_service_account_create, tags: [ 'service-account','roles','firewall'] }
    - { role: gcp_iam_service_account_keys, tags: [ 'keys','roles','firewall'] }
    - { role: gcp_project_services_enable, tags: ['services'] }
    - { role: gcp_iam_policy_bindings_member_serviceaccount, tags: ['roles'] }
    - { role: gce_network_create }
    - { role: gce_firewall_create }
    - { role: gce_instance_create, TARGET_TAGS: ['allow-ssh'], ACCESS_TYPE: "int" }
#    - { role: gce_dynamic_inventory_configure, tags: ['inventory'] }

#  tasks:
#  - name: Create default VPC Network
#    ignore_errors: yes
#    shell: |
#      gcloud compute networks create default \
#        --bgp-routing-mode=global

#  - name: Create control server
#    shell: |
#      gcloud compute instances create control-server \
#        --zone {{ GCP_ZONE }} \
#        --service-account {{ SERVICE_ACCOUNT }} \
#        --shielded-secure-boot \
#        --no-address

#  - name: Build control server image
#    community.docker.docker_image:
#      name: ubuntu
#      repository: gcr.io/

#  - name: Create control server
#    shell: |
#      gcloud compute instances create-with-container control-server \
#        --zone {{ GCP_ZONE }} \
#        --service-account {{ SERVICE_ACCOUNT }} \
#        --container-image {{ CONTROL_SERVER_IMAGE }} \
#        --shielded-secure-boot \
#        --no-address

#  - name: Allow SSH to control-server
#    include_role:
#      name: gce_firewall_create
#
#  - name: Update SSH config
#    shell: |
#      gcloud compute config-ssh

- name: Update runtime environment of instances
  hosts: edge
  gather_facts: yes
  tags: ['python','infra']

  collections:
    - bap_coe.bootstrap_runtime

  roles:
    - python_install_minimal
