---
dependency:
  name: galaxy
driver:
  name: delegated
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: 14-create-apigee-gke-cluster
provisioner:
  name: ansible
verifier:
  name: ansible

