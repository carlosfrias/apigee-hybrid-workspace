---
REGION: "us-central1"
CLUSTER_COUNT: 2
CLUSTER_NAME: "apigee-hybrid-{{ CLUSTER_COUNT }}"
CLUSTER_ZONE: "{{ REGION }}-c"
CASSANDRA_DATA_CENTER_NAME: "dc-{{ CLUSTER_COUNT }}"
CASSANDRA_RACK_NAME: "ra-1"
CASSANDRA_DATA_CENTER_NAME_SEED: "dc-{{ CLUSTER_SEED }}"
CASSANDRA_RACK_NAME_SEED: "ra-1"
CLUSTER_NAME_SEED: "{{ CLUSTER_NAME }}"
CLUSTER_ZONE_SEED: "{{ REGION_SEED }}-c"
REGION_SEED: "us-east1"

