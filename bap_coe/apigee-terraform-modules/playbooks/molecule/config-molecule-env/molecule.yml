---
dependency:
  name: galaxy
driver:
  name: delegated
lint: |
  set -e
  yamllint .
  ansible-lint
  flake8
platforms:
  - name: config-molecule-env
provisioner:
  name: ansible
#  ansible_args:
#  - -u=friasc
#  - --private-key=~/.ssh/google_compute_engine
  playbooks:
#    destroy: destroy.yml
#    create: converge.yml
    converge: converge.yml
verifier:
  name: ansible
  log: True

